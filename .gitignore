node_modules

# npm workspaces uses the single root lockfile — no per-workspace locks
apps/*/package-lock.json
packages/*/package-lock.json

.nx/cache
.nx/workspace-data
.nx/installation

.env

dist

.terraform
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.backup
crash.log

# terraform: local var files carrying alert recipients, gate creds, etc.
*.auto.tfvars
*.auto.tfvars.json
